# CLAUDE.md

## 基本方針
- 目的は、最小変更で素早く実装を進めること
- 不要な確認質問はしない
- まず既存コードを読んで、影響範囲を把握してから編集する
- 関係ないファイルは触らない
- 大規模リファクタは禁止
- TypeScriptは使わない
- Pythonは必ず `python3` を使う
- 日本語で簡潔に返答する
- 制作完了後は必ずdocs/に.mdでGPTに対しての引き継ぎ報告書を記載してください

## Bash運用ルール
- Bash実行は本当に必要な場合だけにする
- 読み取りと編集を優先し、不要なコマンド実行はしない
- build, test, lint, git status などの定型確認は、原則として実行せず、必要ならコピペ可能なコマンドを1つにまとめて提示する
- Bashが必要な場合は、細かく何度も叩かず、必要最小限にまとめる
- 長時間コマンド、破壊的コマンド、副作用のあるコマンドは勝手に実行しない
- 実装後の確認が必要な場合は、まず「実行コマンド」を提示する

## 実装ルール
- 変更は最小限
- 既存設計を尊重する
- 未使用importは消す
- 関係ない整形だけの変更は禁止
- 勝手なライブラリ追加は禁止
- 説明は短く、変更ファイル名を先に出す
- コードはコピペ可能な形で出す

## フルチェック・テスト運用ルール
- ユーザーが「フルチェック」「全体テスト」「入念に確認」「スモークテスト」と指示した場合、画面表示だけで完了扱いにしない
- 検査前に、対象機能・利用権限・主要操作・外部サービス・想定失敗を一覧化し、検査漏れがない状態にする
- 入力機能は、実際に `新規登録 → 保存 → 一覧/詳細への再表示 → 編集 → 必要なら削除または無効化` まで操作する
- 画像・CSV等のファイル機能は、実ファイルの選択、アップロード、保存後表示、差し替え、エラー表示まで確認する
- 外部サービス連携は、環境変数の存在だけでなく、安全なテストデータを使った実接続または提供されたテスト手段まで確認する
- 本番用フロント環境変数はビルド時に埋め込まれるため、設定後の再デプロイと公開ビルドでの動作確認を必須とする
- manager・staff・cast・customer等の関係する全権限で、許可操作と拒否操作の両方を確認する
- 店舗分離対象は、別店舗IDの直接指定でも閲覧・更新できず、拒否時にDBが変更されないことを確認する
- ブラウザ確認では、画面、ネットワーク応答、コンソールエラー、保存後のデータ反映を確認する
- テストデータは実顧客・実電話番号を使わず、識別可能な専用データを使い、確認後に安全に整理する
- 自動テスト成功とブラウザ実操作成功を別々に報告する。実施していない項目を「成功」「確認済み」と書かない
- 外部審査、認証情報不足、実課金等で確認できない項目は、未確認理由と本番開始前の確認手順を明記する
- 完了報告には、実施した操作、対象権限、対象環境、結果、未確認事項、証拠となるスクリーンショットまたはログを含める
